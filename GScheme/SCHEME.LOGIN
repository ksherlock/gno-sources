((lambda ()
   (newline)
   (print "Welcome to GScheme. This 'startup' bit is done by the file")
   (print "SCHEME.LOGIN, which is 'LOAD'ed every time GScheme starts up.")
   (print "You may put any valid Scheme commands in here (except another")
   (print "LOAD command) that you wish executed on startup.")))
